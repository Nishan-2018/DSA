class Solution {
public:
    vector<int> divisors(int n) {
        int i=1;
        vector<int> ans;
        while(i<=n)
        {
            if(n%i==0)
            {
                ans.push_back(i);
                
            }
            i++;
        }
        return ans;
    }
};
