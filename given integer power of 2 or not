class Solution {
public:
    bool isPowerOfTwo(int n) {
        // Your code goes here
        for(int i=0;i<n;i++)
        {
            if(n > 0 &&(n&(n-1)) == 0)
            {
                return true;
            }
        }
        return false;
    }
};
