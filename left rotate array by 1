class Solution {
public:
    void rotateArrayByOne(vector<int>& nums) {
        int lst=nums[0];
        int j=0;
        for(int i=1;i<nums.size();i++)
        {
            nums[j]=nums[i];
            j++;
        }
        nums[nums.size()-1]=lst;

    }
};
