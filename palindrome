class Solution {
public:
    bool isPalindrome(int n) {
        int sum=0,k=0,temp=n;
        while(n>0)
        {
            k=n%10;
            sum=(sum*10)+k;
            n=n/10;
        }
        if(sum==temp)
        {
            return 1;
        }
        
        return 0;
    }
};
